function assignVar() {
    var words = "Was gonna be math but now it isn\'t";
    words += " and now it has more added to it";
    var result = words.fontcolor("red");
    document.getElementById("redWords").innerHTML = result;
}